{
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/**/*.{js,jsx,ts,tsx,json,css,md}": [
      "bun --cwd frontend lint"
    ],
    "backend/**/*.{js,ts,json,md}": [
      "bunx --cwd backend lint",
      "bun --cwd backend typecheck"
    ]
  }
}
