<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Password Reset</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lexend:wght@100..900&display=swap" rel="stylesheet">

    <style>
      :root {
        --primary-900: #1e3a8a;
        --primary-800: #1e40af;
        --primary-700: #1d4ed8;
        --primary-600: #2563eb;
        --primary-500: #3b82f6;
        --primary-400: #60a5fa;
        --primary-300: #93c5fd;
        --primary-200: #bfdbfe;
        --primary-100: #dbeafe;
        --primary-50: #eff6ff;
      }
    </style>

    <script src="https://cdn.tailwindcss.com"></script>

        <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              sans: [ "Lexend", "sans-serif" ]
            },
            colors: {
              primary: {
                DEFAULT: "#5A45FF",
                lighter: "#a599ff"
              },
              primary: {
                900: "var(--primary-900)",
                800: "var(--primary-800)",
                700: "var(--primary-700)",
                600: "var(--primary-600)",
                500: "var(--primary-500)",
                400: "var(--primary-400)",
                300: "var(--primary-300)",
                200: "var(--primary-200)",
                100: "var(--primary-100)",
                50: "var(--primary-50)",
              }
            }
          }
        }
      };
    </script>
  </head>
  <body class="bg-gray-100 py-8">
    <div class="max-w-lg mx-auto bg-white rounded-lg shadow-lg p-8">
      <div class="flex items-center mb-6">
        {{--  <img src="/frontend/public/assets/logo.png" alt="Logo" class="h-10 w-10 mr-2"/>  --}}
        <span class="text-2xl font-bold text-primary-900">Hiphonic</span>
      </div>
      <h2 class="text-xl font-semibold text-primary-700 mb-2">Reset Your Password</h2>
      <p class="text-primary-700 mb-4">
        Hi {{ username }},
      </p>
      <p class="text-primary-700 mb-4">
        We received a request to reset your password. Click the button below to set a new password. This link will expire in 20 minutes.
      </p>
      <div class="text-center my-6">
        <a href="{{ resetLink }}" class="inline-block bg-blue-600 text-white font-semibold px-6 py-3 rounded-lg shadow hover:bg-blue-700 transition">
          Reset Password
        </a>
      </div>
      <p class="text-primary-500 text-sm mb-4 text-center">
        If you did not request a password reset, you can safely ignore this email.
      </p>
      <hr class="my-6 border-gray-200"/>
      <p class="text-xs text-primary-400 text-center">
        &copy; {{ year }} Hiphonic. All rights reserved.
      </p>
    </div>
  </body>
</html>
