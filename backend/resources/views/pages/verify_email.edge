<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{{ title || 'Hiphonic Email' }}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,400..800;1,200..800&display=swap"
        rel="stylesheet">
    <style>
        /* Reset styles for email clients */
        body,
        div,
        p,
        h1,
        h2,
        h3,
        h4,
        h5,
        h6,
        ul,
        ol,
        li {
            margin: 0;
            padding: 0;
        }

        /* Variables */
        :root {
            --navy: #1E2330;
            --dark-navy: #151b25;
            --background: #1E2330;
            --foreground: #f5f5f5;
            --primary: hsl(47.9, 95.8%, 53.1%);
            --primary-rgb: 255, 214, 0;
            --text-primary: var(--foreground);
            --text-secondary: #e1e1e1;
            --footer-bg: rgba(var(--primary-rgb), 0.1);
            --bg-white: var(--navy);
        }

        /* Base styles */
        body {
            font-family: 'Plus Jakarta Sans', system-ui, -apple-system, BlinkMacSystemFont, Oxygen, Ubuntu, Cantarell, 'Helvetica Neue', sans-serif;
            line-height: 1.5;
            color: var(--foreground);
            margin: 0;
            padding: 0;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            background-color: var(--dark-navy);
        }

        /* Typography */
        h1,
        h2,
        h3,
        h4,
        h5,
        h6 {
            font-size: 20px;
            line-height: 25px;
            font-weight: 800;
            margin-bottom: 20px;
            color: var(--foreground);
        }

        p {
            font-size: 16px;
            line-height: 25px;
            font-weight: 500;
            color: var(--foreground);
            margin-bottom: 20px;
        }

        a {
            color: hsl(var(--primary));
            font-size: 16px;
            line-height: 25.2px;
            font-weight: 500;
            text-decoration: underline;
        }

        img {
            border: 0;
            line-height: 100%;
            vertical-align: middle;
        }

        /* Layout containers */
        .email-wrapper {
            width: 100%;
            margin: 0;
            padding: 20px;
            background-color: var(--dark-navy);
        }

        .email-container {
            margin: 0 auto;
            max-width: 960px;
            padding: 30px 18px;
            background-color: var(--navy);
            border-radius: 8px;
        }

        .logo-container {
            width: fit-content;
            margin: 0 0 29px 0;

            display: flex;
            justify-content: center;
            align-items: center;
            background: hsla(47.9, 95.8%, 53.1%, 0.9);
            padding: 16px;
            border-radius: 4px;
        }

        .logo-container h2 {
            color: var(--bg-white);
            margin: 0;

        }

        .content {
            margin-bottom: 20px;
        }

        .footer {
            margin: 0 auto;
            max-width: 945px;
            background-color: var(--dark-navy);
            margin-top: 57px;
            padding: 24px;
            border-radius: 4px;
        }

        .footer p {
            color: var(--text-secondary);
            font-size: 14px;
            font-weight: 400;
            line-height: 18.2px;
            margin: 0;
        }

        .footer a {
            color: hsl(var(--primary));
            text-decoration: none;
            font-size: 14px;
            font-weight: 400;
            line-height: 18.2px;
        }

        /* Utility classes */
        .btn {
            background-color: hsl(var(--primary));
            color: var(--navy);
            padding: 10px 20px;
            border-radius: 4px;
            text-decoration: none;
            font-weight: bold;
            display: inline-block;
        }

        .text-center {
            text-align: center;
        }

        .mb-0 {
            margin-bottom: 0;
        }

        .mt-0 {
            margin-top: 0;
        }

        /* Media Queries */
        @media only screen and (max-width: 600px) {
            .email-container {
                padding: 20px 12px;
            }

            p,
            a {
                font-size: 15px;
                line-height: 24px;
            }

            .footer {
                margin-top: 40px;
                padding: 20px;
            }
        }

        /* Dark mode support - simplified since we're already using dark theme */
        @media (prefers-color-scheme: light) {

            .email-wrapper,
            .email-container {
                background-color: var(--navy);
            }

            body,
            p,
            h1,
            h2,
            h3,
            h4,
            h5,
            h6 {
                color: var(--foreground);
            }

            .footer {
                background-color: var(--dark-navy);
            }

            .footer p {
                color: var(--text-secondary);
            }
        }

        /* OTP styles */
        .otp-container {
            background-color: var(--dark-navy);
            padding: 15px;
            text-align: center;
            margin: 20px 0;
            border-radius: 4px;
            border: 1px solid rgba(var(--primary-rgb), 0.2);
        }

        .otp-text {
            letter-spacing: 5px;
            color: hsl(var(--primary));
            font-size: 32px;
            font-weight: 700;
            margin: 0;
        }

        .litigation-details {
            display: flex;
            align-items: center;
            gap: 5px;
        }
    </style>
</head>

<body>
    <div class="email-wrapper">
        <div class="email-container">
            <div class="logo-container">
                <h2>Zeph Chambers</h2>
            </div>
            <div class="content">
                <h2>Welcome to {{ appName }}!</h2>
                <p>Hi {{ fullName }},</p>
                <p>Thank you for registering with us. To complete your registration, please verify your email using the
                    OTP below:
                </p>
                <div style="background-color: #f4f4f4; padding: 15px; text-align: center; margin: 20px 0;">
                    <h1 style="letter-spacing: 5px; color: #444;">{{ otp }}</h1>
                </div>
                <p>This OTP will expire in 5 minutes.</p>
                <p>If you didn't create an account with us, please ignore this email.</p>
                <p>Kind regards,<br>Zeph Chambers Team</p>
            </div>


        </div>
    </div>
</body>

</html>